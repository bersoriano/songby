import styled from 'styled-components';

const StyledYearList = styled.ul`
    position: relative;
    overflow-y: scroll;
    max-height: 100px;
    list-style: none;
    li {
        padding: 5px 0;
    }
`;

export default StyledYearList;