import styled from 'styled-components';

const SongList = styled.ul`
    padding: 0 40px;
    li {
        font-size: 20px;
        margin: 10px 0;
        list-style: none;
    }
`;

export default SongList;