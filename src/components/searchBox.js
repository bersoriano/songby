import styled from 'styled-components';

const SearchBox = styled.input`
    margin: 10px 20px;
    min-height: 40px;
    font-size: 25px;
    padding: 0 15px;
    border-radius: 30px;
    outline: none;  
`;

export default SearchBox;