import styled from 'styled-components';

const InputLabel = styled.label `
    color: white;
    text-align: left;
    margin: 0 20px;
    font-weight: 500;
    font-size: 15px;
`;

export default InputLabel;